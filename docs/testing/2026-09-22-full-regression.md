# 2026-09-22 全量测试及异常记录

用户明确要求全量，不按阶段交付。范围包含全部 Vitest、数据库、存储、原生窗口、Electron 页面专项、成熟度验收、通知后台联调和缩略图基准；辅助脚本由所属测试调用，不单独当作测试。

## 后续修复：F02 帮助中心崩溃已修复（2026-09-22 16:24）

用户授权先修复帮助中心。本次仅修改帮助快捷入口及直接相关测试，不修改拖动、层级或日志业务。

- 将失效的 `notes-move` 入口替换为现有 `note-duration`（便签持续方式）。快捷入口解析独立为 `help-quick-links.js`，缺少文章时不向模板传入 undefined，避免单个入口使整个页面崩溃。
- 新增 3 项快捷入口单测：配置 ID 必须有效且唯一、持续方式替换正确、部分/全部文章缺失时安全返回。配置有效性断言防止容错逻辑掩盖以后出现的失效入口。
- 帮助 Electron 专项增加三个视图中的四个快捷入口及持续方式文章跳转/焦点验证。原有首页、文字选择、搜索、输入法、阅读位置、布局和明暗主题测试继续保留。
- 局部单测：`help-quick-links`、`help-search`、`help-navigation-ui`、`help-support-ui`、`help-scroll-motion`，5 文件共 13 项全部通过。
- 修改文件 ESLint、应用构建和 `git diff --check` 通过；无 CSS/布局体系变更。查看了本轮白底、黑底、复杂背景截图。
- 真实 Electron：月视图完整专项 28.435 秒、周视图完整专项 4.249 秒、帮助中心完整专项 27.811 秒，均退出 0；此前因帮助崩溃而未执行的月/周后续断言本次已跑完。
- 本轮原始日志、截图、起止哈希和结果：`tmp/test-runs/2026-09-22T08-22-57-152Z-DJN0L4/`。起止登记文件哈希无差异；未覆盖或删除之前失败证据。
- 月专项中的“已完成便签的生效时间不可修改”等错误为脚本主动验证的拒绝场景，对应断言通过；本次没有新增失败断言。
- 本次未重跑全量 51 项、未打安装包，未验证安装升级、多屏/DPI或测试用户的偶发拖动问题。F02 修复不代表 F01 已修复。

## 全量执行当时的结果摘要（保留历史，F02 最新状态见上）

**不是全绿：确认 1 个未修复的帮助中心功能缺陷，影响 3 个专项。** 测试自身的 5 类问题已修正并复测。没有把失败改为跳过，没有因某项失败而停止后续独立专项。

| 运行 | 结果 | 证据目录（均在 `tmp/test-runs/`） |
| --- | --- | --- |
| 首轮完整 51 项 | 43 通过、8 失败；单元 616/617 | `2026-09-22T07-37-09-867Z-urGku6` |
| 第二轮完整 51 项 | 47 通过、4 失败；单元 626/626 | `2026-09-22T07-54-01-152Z-3vvSQa` |
| 最后单元、两类 lint、重建应用及日志专项 | 5/5 项通过；单元 623/623（108 文件） | `2026-09-22T08-00-16-520Z-AyMATC` |

第二轮 4 个失败中，日志专项的布尔序列化预期已由并行任务修正并在最后一轮通过；其余 3 个是 F02。不能将跨轮合并的通过结果写成“一次 51/51 全绿”。完整回归命令已执行两次，不存在未启动的登记专项；失败脚本内部被中断的后续步骤仍未覆盖。

已执行：全部单元、数据库（包含 5 万条数据验收）、附件/壁纸存储、Windows 原生与全部登记 Electron 专项、成熟度验收、通知客户端/本地后台联调、缩略图基准、原生 DLL 和前端/主进程/通知预览构建、源码及测试 lint、`git diff --check`。最新测试 lint 无错误/警告；最新源码 lint 无错误、有 3 条并行日志模块的格式警告，未擅自重写该模块。

第二轮缩略图基准：100 次请求 17 ms，缓存失效和并发去重均通过。5 万条数据日历查询 76 ms、搜索 16 ms；仅是本机测试样本，不作跨设备性能承诺。

### 工作区一致性边界

首轮起止业务源码未改变（仅本任务修改了已执行完的白名单测试）。第二轮和最后复测期间，另一日志任务仍在修改源码、测试和输出构建：起止哈希记录了这些差异。因此本报告是有版本/时点证据的执行记录，**不是冻结提交或最终安装包的发布签字**。

单测数量 626 → 623 的变化并非本任务删掉失败断言；两轮清单显示 `tests/ipc-action-context.test.js` 在并行日志重构中移除，其他日志断言亦在调整。本任务没有删除该文件。已保存每轮实际清单和结果，不把不同工作区状态的数量混算。

## 证据与执行原则

- 首轮证据目录：`tmp/test-runs/2026-09-22T07-37-09-867Z-urGku6/`。
- 每项包含 stdout/stderr、命令、退出码、耗时；根目录包含逐项汇总和工作区起止文件哈希。重跑新建目录，不覆盖首次失败。
- 原生窗口串行执行；直接使用工作区 Node，真实 Electron 在沙箱外执行，等待实际进程结束。
- 失败不阻止无依赖的后续测试；编译失败则将相关测试记为 blocked，不使用旧构建冒充新结果。
- 非零退出需要人工判断是功能异常、测试缺陷还是依赖问题。预期的错误注入不算功能失败；退出零也不代表日志里每条错误都可忽略。
- 首轮开始时工作区已有功能与诊断日志改动；报告针对当时工作区，不等同于 HEAD 或已发布安装包。

## 异常清单（持续更新）

### T01：贴边 IPC 白名单测试误报（测试缺陷）

- 首轮：108 个单测文件、617 个断言，616 通过，1 失败。
- 失败：`tests/dock-main-wiring.test.js` 的原子 dock IPC 测试，断言白名单不含 `dock.revealHandleMode`。
- 证据：首轮 `unit/stderr.log`、`unit/stdout.log`。
- 原因：测试使用已不存在的 `const APPLICATION_SETTING_IDS` 作为源码截取终点，`indexOf` 返回 -1，截取到主进程文件末尾，误将专用 dock handler 中合法字段算进通用白名单。
- 修改：只提取实际 `new Set([...])` 声明并验证声明存在，按字段列表断言，保留禁止通用逐字段写入的安全约束。
- 复测：`2026-09-22T07-45-18-450Z-QarErj/unit` 中本项通过；同轮新增的日志协议断言失败另记 L01，不能混为本项未修复。

### F01：置底退出时重入 show 回调，置顶同步失败（本轮未复现）

- 专项：`tests/window-z-order-recovery-electron.mjs`。
- 步骤：正常层级 → 置底 → 置顶，同时在第一次原生置顶调用内注入 show 事件，检查原生窗口和模糊层同步。
- 之前观察到“窗口层级未能同步，请重新选择后重试”。本报告以本轮原始日志为最终证据，不把有意拒绝全部置顶请求的前序断言当作异常。
- 状态：首轮 24.368 秒、复测 `2026-09-22T07-45-18-450Z-QarErj` 24.369 秒均通过。两个周期的无模糊 IPC 拖动专项也通过。没有修改、跳过该失败场景；不能由本机两次通过推导测试用户的偶发拖动故障已修复。

### F02：帮助中心无法渲染（首次确认的功能缺陷，后续已修复）

- 复现：列表/月/周任一主视图点击帮助入口。
- 现象：帮助正文没有打开；`HelpPage` 报 `Cannot read properties of undefined (reading 'id')`。
- 证据：首轮 `tests_help-center-electron.mjs/stderr.log`，以及该项保留的 `temp-*/abandon-help-center-*/logs/app-2026-09-22.jsonl` 第 14 条 `vue.error`。同轮月、周专项分别报“帮助中心没有完成打开”。
- 根因：`HelpPage.vue` 的 quickLinks 仍查找 `notes-move`，而 `help-content.js` 已不包含此文章；数组出现 undefined，模板 `article.id` 访问崩溃。编译后的 `HelpPage-Cd6CEMfm.js:1302` 与应用错误堆栈对应。
- 首次范围：三个帮助相关脚本在此处停止，之后的断言当时未执行，不能记为通过。用户后续授权修复，本页顶部保留具体修改和完整复测结果。

### T02：主视图切换持久化断言抢跑（测试缺陷，已修正）

- 首轮 `tests_main-view-enhancements-electron.mjs`：读到 week，预期 list。
- 主进程在 `presentActiveViewInExistingWindow()` 完成之后写入 active_view；页面 DOM 先出现，不表示 IPC/展示确认和 SQLite 写入已完成。
- 修正：有界轮询实际 SQLite 值，最终仍断言 list，并在 finally 关闭只读连接；不只检查渲染快照。
- 复测：`2026-09-22T07-45-18-450Z-QarErj` 通过。

### T03：月历文字颜色选区落到空文本节点（测试缺陷，已修正）

- 首轮 `tests_month-event-text-color-electron.mjs` 的应用日志记录 `IndexSizeError`，Range offset 2 大于 node length 0。
- 原因：TreeWalker 首节点可能是 Vue 的空文本节点，并非正文。
- 修正：定位包含完整测试正文的文本节点；仍验证“详情”的范围、SQLite 持久化、详情与日期格颜色同步。
- 复测：`2026-09-22T07-45-18-450Z-QarErj` 通过。

### T04：周视图预览测试缺少真实便签（测试缺陷，已修正）

- 首轮为笼统的 Script failed to execute；增加表达式跟踪后，在 `2026-09-22T07-47-17-801Z-wKejkf` 精确定位到点击“展开左侧操作列表”。
- 原因：空日期本来就禁用此入口，aria-label 是“当天没有可操作的真实便签”，不是正常展开按钮；等待更久无效。首次选中日期也可能为空，不能用空 data-date 定位快速创建入口。
- 修正：先断言空日期按钮禁用，再通过真实 API 为所选日期新建便签，等待入口启用，保留展开/关闭/快速创建/数量预览整条链；后续操作使用明确选择的 targetKey。
- 复测：`2026-09-22T07-50-55-780Z-5Bqjnl` 通过，2.875 秒。

### T05：成熟度测试误点被遮挡的新建器（测试缺陷，已修正）

- 首轮 `tests_maturity-electron.mjs` Escape 后焦点断言失败；其报告另存为首轮 case 内 `initial-results.json`。
- 复测添加等待仍失败；`2026-09-22T07-48-44-782Z-4S7Nqj` 的 stdout 显示打开的是“持续方式”选项，不是模板筛选频率。
- 根因：全局 `.sel-trigger` 在新建便签加入持续方式后命中底层隐藏控件；通过 JS click 可点到真实用户无法点击的位置。
- 修正：限定 `.tp-filter-shell .sel-trigger`，等待焦点进入真实面板，验证 Escape 关闭并回焦。不修改控件功能。
- 复测：`2026-09-22T07-50-55-780Z-5Bqjnl` 通过，5.627 秒，后续标签弹层及退出草稿断言也执行。

### L01：并行日志改造期间的协议断言失败（工作区变化，单独追踪）

- 首轮没有这些失败；`2026-09-22T07-45-18-450Z-QarErj/unit` 出现 4 项失败，涉及 `diagnostic-ipc`、`sticky-diagnostic-ipc`、`ipc-arguments`。
- 实际差异：诊断终态由 success 变为 returned；正文由原文/截断预览变为 private-field 脱敏。对应代码在另一任务中持续修改，首次日志和起止哈希已保留。
- 未将当前语义简单改回旧值，也未改对方正在维护的测试以强制通过。最终结果以末轮完整执行为准。
- 第二轮日志 Electron 断言要求 `stored === 'false'`，而原始 `settings.persisted` 证据显示同一个 actionId、outcome verified、requested/stored 均为 `'0'`、matches true，设置快照为 boolean false：这是断言未遵循既有数据库布尔序列化，并非保存失败。并行任务随后修为 `'0'`。
- 最后 `2026-09-22T08-00-16-520Z-AyMATC` 重建与日志专项通过，覆盖真实设置读回、静默写入不一致注入、便签修改与页面应用关联、业务拒绝、两个日志导出范围。原始错误日志仍保留。

### R01：日志专项结束时 Chromium GPU 告警（已记录，未据此中止）

- 最后日志专项已经输出完整业务断言通过标记，退出码为 0；stderr 随后出现两条 `GPU state invalid after WaitForGetOffsetInRange`。
- 证据：最后一轮 `tests_logging-actions-electron.mjs/stderr.log`。本次不是 GPU 启动失败，没有用它解释或掩盖帮助中心的业务失败。
- 未发现它导致本专项功能断言失败，但本轮不覆盖该告警的长期图形稳定性影响。

## 测试治理改动

- 增加 `test:full:win`：运行完整登记的 45 个独立 mjs 专项（包含成熟度 DB 和 GUI、通知后台、基准），以及单元、lint、原生/应用/通知预览构建，共 51 项任务。
- 登记检测递归覆盖 tests 子目录；排除 helper，防止漏跑、重跑或将辅助进程当独立测试。
- 公共运行器每次创建不可覆盖的证据目录、真正等待 Electron 退出、超时只终止自己启动的子进程树、独立项目失败后继续、依赖失败记 blocked。
- 每项临时目录隔离，成功后删除本次缓存，失败保留；应用 JSONL 在清理前拷贝。旧测试若自行提前清理，可能仅剩 stdout/stderr，此边界未隐藏。
- 删除 `lint:tests` 的 `--quiet`，将首轮 187 条测试格式警告按 Prettier 修复，没有删除业务断言。
- 原有有价值的专项仍保留，未按“源码字符串断言”标签批量删除。首次“37 个纯源码断言文件”估算方法不严谨，本报告不采用该数字。

## 尚未代表的验证

本机自动化不等于 Windows 10/11 双系统、不同 DPI/多屏、安装升级、真实用户鼠标快速操作及长时间稳定性验收。拖动专项直接调用真实 preload/IPC 并检查原生位置，不等于物理鼠标事件链已全部验证。
