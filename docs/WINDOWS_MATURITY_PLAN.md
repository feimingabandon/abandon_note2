# Windows 应用成熟度整改计划

日期：2026-09-09。整改前回滚基线：`5333e402b4fd6ff4e0747174b44a556dcafd0194`。

## 产品决定（项目记忆）

- 整体备份与恢复留待后续开发，本轮不实现；普通便签撤销删除与编辑草稿保护属于本轮。
- 首次进入的作者故事、赞赏、按钮文案及流程是刻意设计，保持现状。
- 首次未经提前同意发送设备统计是刻意设计，保持现状。
- 更新下载来源与现有 GitCode/GitHub 策略暂时保持现状。
- 本轮完善 Windows 10/11；不制作、发布或推广 macOS 安装包。

## 实施与验收顺序

| 阶段 | 具体实现 | 验收条件 | 状态 |
| --- | --- | --- | --- |
| 0 回滚基线 | 提交现有项目源码、配置、测试、工具；保留本地临时文件 | 基线提交可查，整改变更可独立对比 | 已完成 |
| 1 数据保护 | 共享草稿保护覆盖新建、完整编辑、快速编辑、模板、日历草稿；视图切换和退出协商；快速编辑比较打开时正文，冲突保留草稿 | 托盘/标题栏切换和退出不会静默丢稿；并发更新不被旧正文覆盖；恢复草稿不自动覆盖数据库 | 已实现；专项结果见验收记录 |
| 2 撤销删除 | 数据库、IPC、preload、搜索结果恢复入口；保持附件和标签；明确过期预约、提醒和便利贴行为 | 删除后可以在包含已删除的搜索中恢复；彻底删除不可恢复；帮助与行为一致 | 已实现；专项结果见验收记录 |
| 3 故障恢复 | 主渲染崩溃有限重试与可用的手动恢复入口；启动错误提示；核心调度退避恢复 | 不陷入无限重载；错误有诊断信息；瞬时失败恢复后核心任务继续执行 | 已实现；专项结果见验收记录 |
| 4 共享交互 | IME 保护；选择/标签浮层翻转及视口限制；键盘导航、关闭、焦点恢复；可聚焦搜索结果操作 | 中文候选确认不提交；240px 窄窗和底部触发器浮层可用；模态内 Teleport 不破坏焦点管理 | 已实现；专项结果见验收记录 |
| 5 信息与视觉 | 设置搜索及当前/所有视图范围说明；状态改为待开始；主按钮前景和焦点令牌；滚动可发现性与动效令牌 | 自定义文字色下主按钮可读；键盘焦点可见；可查到设置及影响范围；保留首启内容 | 已实现；专项结果见验收记录 |
| 6 容量与性能 | 关联 SQL 分批；日历提前过滤；日报精简查询；记录搜索/图片基准，按证据决定缓存、并发及按需加载 | 33000 候选不再超 SQL 参数上限；搜索结果等价；缓存有上限与失效规则；无依据不改 FTS/分页协议 | 已实现；专项结果见验收记录 |
| 7 工程收口 | Vitest 仅发现正式 tests；抽出本轮新增的独立职责；更新帮助和验收记录 | 指定测试不收集 tmp 副本；专项测试和必要构建通过；明确未覆盖风险 | 已实现；专项结果见验收记录 |

## 计划复核

- 原报告中的备份、首启文案、统计选择、更新来源及 macOS 建议已按用户决定排除，不作为验收失败项。
- 草稿保存与数据库提交分离，冲突必须保留用户正文；破坏性操作继续保留确认。
- 崩溃恢复依赖草稿保护，先完成数据保护再打开自动恢复。
- UI 改动遵守 `UI_DESIGN_STANDARD.md` 的表面、边线、层级及应用自身动效规则。
- 公共组件需检查列表、月、周及模板/日志复用入口；不以单处修补冒充共享修复。
- 只执行命名的局部测试和必要构建、数据库/Electron 专项；不默认执行完整回归。
- 真实混合 DPI、长时间运行和安装包发布验收必须如实记录，不以模拟尺寸替代真机验收。

复核结论：范围与依赖顺序可执行，进入实现。每阶段记录实际验证与剩余事项，不把计划项标为已完成直到实现和相应验证结束。

## 最终执行结果

本轮 1—7 阶段实现完成，局部验收通过；测试边界、容量/性能数字及尚未覆盖的风险见 [验收记录](WINDOWS_MATURITY_ACCEPTANCE.md)。本轮未打包、发布或推送远端。
