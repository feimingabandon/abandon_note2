# Abandon Note 2：Electron 43 升级与桌面便利贴实施文档

> 文档版本：v1.0
> 审查日期：2026-07-24
> 适用项目：`abandon_note2`
> 目标 Electron：`43.2.0`
> 目标平台：Windows 10、Windows 11、macOS Intel、macOS Apple Silicon
> 文档状态：升级与开发执行基线

---

## 1. 文档目标

本文档用于指导以下两项工作：

1. 将当前项目从 Electron 39 升级到 Electron 43。
2. 在升级完成并验证稳定后，实现低占用的桌面便利贴模块。

两项工作必须分阶段进行，不建议在同一个提交中同时修改 Electron 版本和开发便利贴功能。

推荐顺序：

```text
阶段 A：Electron 39 → Electron 43
    ↓
现有功能完整回归
    ↓
阶段 B：便利贴共享 Renderer 技术验证
    ↓
阶段 C：便利贴完整功能开发
    ↓
多窗口性能与多屏幕验收
```

---

# 第一部分：当前项目审查结论

## 2. 当前主要依赖

本次仓库静态审查确认，项目当前主要版本为：

| 依赖 | 当前版本 |
|---|---:|
| Electron | `package.json` 为 `^39.2.6`，锁文件实际为 `39.8.9` |
| better-sqlite3 | `12.9.0` |
| koffi | `2.16.2` |
| electron-vite | `5.0.0` |
| electron-builder | `26.8.1` |
| Vite | `7.3.2` |
| Vue | `3.5.33` |
| 本地 Node | Volta 固定为 `22.22.3` |

## 3. 是否存在 Electron 39 专属依赖

审查结果：

> 当前项目没有发现必须停留在 Electron 39 的代码级原因。

现有主要 Electron API 包括：

```text
BrowserWindow
Tray
Menu
Notification
screen
desktopCapturer
ipcMain
nativeImage
app.requestSingleInstanceLock
app.setAppUserModelId
app.setAsDefaultProtocolClient
webContents.setWindowOpenHandler
BrowserWindow.getNativeWindowHandle
```

这些能力在 Electron 43 中仍然存在。

项目中没有发现：

- Electron 39 版本号判断；
- Electron 39 ABI 硬编码；
- 针对 Electron 39 的 `patch-package`；
- 已被 Electron 40～43 删除的核心 API；
- 已废弃的 `remote` 模块；
- `BrowserView`；
- Renderer 直接导入 Electron 的 `clipboard`；
- Electron 39 专属命令行参数；
- Electron 39 专属 preload 实现。

现有复制功能使用：

```js
navigator.clipboard.writeText(text)
```

这是浏览器标准 API，不属于 Electron 40 弃用的 Renderer `clipboard` 模块调用，因此无需因为升级而修改。

---

# 第二部分：Electron 43 升级方案

## 4. 升级目标

固定升级到：

```json
{
  "devDependencies": {
    "electron": "43.2.0"
  }
}
```

截至本文档日期，Electron 43.2.0 是最新稳定版，包含 Chromium 150、Node.js 24.17.0 和 V8 15.0（具体补丁号以实际 `process.versions` 为准）。

首次升级必须使用精确版本，不使用：

```json
"electron": "^43.2.0"
```

原因是需要先锁定完整测试环境，避免后续补丁版本自动变化影响原生模块和打包结果。

---

## 5. 推荐依赖版本

第一阶段仅修改必要依赖：

```json
{
  "dependencies": {
    "better-sqlite3": "12.11.2",
    "koffi": "2.16.2"
  },
  "devDependencies": {
    "electron": "43.2.0",
    "electron-builder": "26.8.1",
    "electron-vite": "5.0.0",
    "vite": "7.3.2"
  },
  "volta": {
    "node": "22.22.3"
  }
}
```

本次升级暂不修改：

- electron-vite；
- electron-builder；
- Vite；
- Vue；
- 本地 Node；
- 其他业务依赖。

这样可以减少同时发生变化的变量。

---

## 6. better-sqlite3 处理

当前 `better-sqlite3 12.9.0` 不建议直接用于 Electron 43。

推荐固定为：

```json
"better-sqlite3": "12.11.2"
```

`better-sqlite3 12.11.2` 官方发布记录明确增加了 Electron 43 预构建支持。

暂时不升级到 `12.12.0`，因为该版本开始要求 Electron 43 的 Linux 预构建二进制运行在 glibc 2.41 或更高环境。当前项目的构建配置仍包含 Linux 目标，直接升级可能影响旧 Linux 发行版。

暂时也不升级到 `13.x`。`13.0.0` 刚切换到 N-API，虽然理论上能提高不同 Node/Electron 版本之间的二进制兼容性，但属于原生绑定架构的大版本变化，应在 Electron 43 升级完成后单独评估。

推荐顺序：

```text
本次升级：
Electron 43.2.0
better-sqlite3 12.11.2

后续单独任务：
评估 better-sqlite3 13.x
```

---

## 7. Koffi 与原生毛玻璃

当前项目通过以下路径调用 Windows 原生毛玻璃：

```text
Electron BrowserWindow
    ↓ getNativeWindowHandle()
Koffi
    ↓
blur_engine.dll
    ↓
Windows 原生窗口与 DirectComposition
```

这一实现没有直接绑定 Electron 39 ABI。

`blur_engine.dll` 接收的是原生窗口句柄，Electron 43 仍然提供 `getNativeWindowHandle()`，因此不需要因为升级 Electron 而重写 DLL。

建议固定当前已安装版本：

```json
"koffi": "2.16.2"
```

不要继续使用过宽的版本范围。

升级后必须回归：

- 原生模糊初始化；
- HWND 获取；
- 主窗口移动；
- 主窗口缩放；
- 贴边隐藏；
- 从托盘恢复；
- 模糊强度修改；
- Overlay 层级；
- 多次开启与关闭模糊；
- 应用退出时原生线程释放；
- Windows 10；
- Windows 11。

---

## 8. electron-builder 配置修改

当前配置包含：

```yaml
asarUnpack:
  - resources/**

npmRebuild: false
```

建议修改为：

```yaml
asarUnpack:
  - resources/**
  - node_modules/better-sqlite3/**
  - node_modules/koffi/**
```

并删除：

```yaml
npmRebuild: false
```

即恢复 electron-builder 默认的原生依赖重建行为。

> 补充说明：electron-builder 在默认 `npmRebuild` 行为下会自动将含 `.node` 的原生依赖解压到 ASAR 外，因此上方显式列出 `better-sqlite3`/`koffi` 属于**显式兜底、非强制**，但保留可避免因自动探测失效导致的打包问题。
>
> 另注意：当前 Windows 原生毛玻璃 `blur_engine.dll` 是通过 `electron-builder.yml` 的 `extraResources`（`from: native_blur/build/bin/` → `to: native_blur/`）打包，而非 `asarUnpack`；两条路径职责不同，不要混淆。

Electron 和普通 Node 的原生模块 ABI 不完全相同。Electron 官方建议原生 Node 模块按照目标 Electron 版本重新构建。

项目已有：

```json
"postinstall": "electron-builder install-app-deps"
```

应继续保留。

### 修改后的原则

- `better-sqlite3` 保持 external；
- 不允许 Rollup 将 `.node` 文件打进 JavaScript；
- 原生模块必须位于 ASAR 外；
- Windows 构建在 Windows 环境执行；
- macOS x64 与 arm64 应分别构建或生成 Universal 包；
- 不在一个平台直接生成另一个平台的原生模块。

---

## 9. 推荐升级命令

先创建独立分支：

```bash
git checkout -b chore/electron-43-upgrade
```

更新依赖：

```bash
npm install --save-dev --save-exact electron@43.2.0
npm install --save --save-exact better-sqlite3@12.11.2
npm install --save --save-exact koffi@2.16.2
```

不建议删除 `package-lock.json`，否则可能导致大量无关依赖同时升级。

依赖更新完成后，可以清理安装目录并严格根据锁文件重新安装。

PowerShell：

```powershell
Remove-Item -Recurse -Force node_modules
npm ci
```

然后执行原生依赖安装：

```bash
npm run postinstall
```

出现 `NODE_MODULE_VERSION` 或 ABI 不匹配时，可进一步执行：

```bash
npx electron-builder install-app-deps
```

必要时使用 Electron 官方推荐的 `@electron/rebuild` 单独重建 `better-sqlite3`：

```bash
npx @electron/rebuild -f -w better-sqlite3
```

---

## 10. Electron 42 之后的下载机制

Electron 42 开始，Electron npm 包不再在自身 `postinstall` 阶段下载二进制，而是在第一次运行 Electron 命令时执行延迟下载，同时移除了 `ELECTRON_SKIP_BINARY_DOWNLOAD`。Electron 也提供了 `install-electron` 脚本用于主动下载。

项目目前没有发现 `ELECTRON_SKIP_BINARY_DOWNLOAD`，但仍需检查：

- CI 环境变量；
- Gitee 流水线；
- GitHub Actions；
- 本地安装脚本；
- 发布脚本；
- Dockerfile；
- README 安装说明。

可在 CI 中主动触发：

```bash
npx install-electron
```

项目已有 Electron 镜像配置，升级后必须验证：

```text
electron_mirror
electronDownload.mirror
```

能否继续正确下载 Electron 43。

---

## 11. macOS 通知注意事项

项目当前使用 Electron 系统 `Notification`。

> 当前代码现状（已落地，以代码为准）：下列三处通知均已添加 `failed` 监听，**失败时记录日志并降级为应用内消息条**（确认弹窗体验太重，已放弃）：
>
> - `src/main/index.js` 的 Windows 富通知（`toastXml`）；
> - `src/main/index.js` 的降级通知（mac/Linux 或缺少 noteId）：`new Notification({ title, body, silent, icon, actions?, closeButtonText? })`；
> - `src/main/services/scheduler.js` 的终极告警通知：`new Notification({ title, body, urgency })`。
>
> 降级链路：`failed` → `notifyNotificationFailure()`（`console.error` + `mainWindow.webContents.send('app:message', ...)`）→ 渲染进程应用内消息条；scheduler 通过 `onAlertNotifyFailed` 回调接入同一链路。

Electron 42 开始，macOS 通知切换到 `UNNotification`，应用必须经过代码签名才能正常展示通知；未签名应用会触发 Notification 的 `failed` 事件。

### mac/Linux 富通知（与 Windows 交互对齐）

Windows 富通知依赖 `toastXml`（Windows 专属），mac/Linux 无法使用。对存在有效便签 id 的通知，代码改用跨平台 `actions` 形成等效交互：点击正文打开并聚焦主窗口，点击「稍后提醒（10分钟）」触发 `snoozeNote`。

```js
const options = { title, body, silent, icon }
if (hasNote) {
  options.actions = [{ type: 'button', text: '稍后提醒（10分钟）' }]
  options.closeButtonText = '明白'
}
const notification = new Notification(options)
notification.on('click', () => { /* 打开并聚焦主窗口 */ })
notification.on('action', (_event, index) => { /* index === 0 → snoozeNote */ })
notification.on('failed', (_event, error) => {
  notifyNotificationFailure('系统通知', title, body, error)
})
notification.show()
```

注意：macOS 需应用签名后 `actions` 按钮才生效；未签名 / 开发态会触发 `failed`，自动落到应用内消息条。样式由系统渲染（主按钮 + 关闭按钮，多余动作收进下拉菜单），只能做到等效而非与 Windows 完全一致。

### 失败降级：日志 + 应用内消息条

所有通知增加 `failed` 监听，**失败时记录日志并下发应用内消息条**（不弹确认弹窗，避免体验过重）：

```js
function notifyNotificationFailure(scene, title, body, error) {
  console.error(`[notification] ${scene}发送失败，降级为应用内消息条:`, error)
  if (!mainWindow || mainWindow.isDestroyed()) return
  const text = `${title}：${String(body || '')}`.trim().slice(0, 80) || title
  mainWindow.webContents.send('app:message', { type: 'warning', text, duration: 6000 })
}
```

开发态未签名的 macOS 应用通知失败时：

- 不应导致调度任务失败；
- 不应导致主进程崩溃；
- 记录日志并降级为应用内消息条（依赖主窗口存活；窗口隐藏/最小化/收进托盘时消息条虽已下发，但用户当下可能看不到）。

正式发布前应验证：

- macOS Intel 签名包；
- macOS Apple Silicon 签名包；
- 通知权限首次请求；
- 用户拒绝通知；
- 应用关闭主窗口后托盘常驻时的通知；
- 点击通知重新打开应用。

---

## 12. Node 版本说明

项目本地 Node 使用：

```text
22.22.3
```

Electron 43 内置 Node 使用：

```text
24.17.0
```

这两个 Node 版本用途不同：

| Node | 用途 |
|---|---|
| 本地 Node 22.22.3 | npm、electron-vite、Vite、electron-builder |
| Electron 内置 Node 24.17.0 | Electron 主进程、preload、`ELECTRON_RUN_AS_NODE` |

当前本地 Node 22.22.3 可以继续使用，不需要为了 Electron 43 强制升级到 Node 24。

项目中使用 `ELECTRON_RUN_AS_NODE=1` 的测试脚本会在 Electron 内置 Node 24 下运行，因此必须重新执行所有相关测试。

---

## 13. Electron 40～43 关键变化摘要

### Electron 40

- Chromium 升级到 144；
- Node 升级到 24.11.1；
- Renderer 直接使用 Electron `clipboard` 被弃用；
- 子进程退出原因增加 `memory-eviction`。

当前项目使用 `navigator.clipboard`，无需修改。

### Electron 41

- Chromium 升级到 146；
- Node 升级到 24.14.0；
- 增加 macOS ASAR Integrity；
- 改善无边框窗口和部分平台行为。

当前项目主要影响较小。

### Electron 42

- Chromium 升级到 148；
- Node 升级到 24.15.0；
- macOS 通知改用 `UNNotification`；
- Electron 二进制改为首次运行时下载；
- 移除 `ELECTRON_SKIP_BINARY_DOWNLOAD`；
- 增加 Renderer OOM 调用栈；
- 改善无边框窗口边缘缩放；
- 增加动态菜单标签更新能力。

### Electron 43

- Chromium 升级到 150；
- Node 当前稳定补丁为 24.17.0；
- 新增 `webContents.clone()`；
- 增加 Renderer OOM 诊断；
- 改善主进程、preload、Renderer 启动性能；
- 可显式控制无边框窗口直角；
- 提供多项运行时和 IPC 性能优化。

其中 `webContents.clone()` 是本便利贴方案选择 Electron 43 的主要原因。

---

# 第三部分：Electron 43 升级执行流程

## 14. 阶段 A：建立升级基线

升级前记录：

- [ ] 当前 Git commit；
- [ ] 当前 `package-lock.json`；
- [ ] 当前 Electron 39 可运行安装包；
- [ ] 当前数据库样本；
- [ ] 当前 Windows 10 运行结果；
- [ ] 当前 Windows 11 运行结果；
- [ ] 当前 macOS 运行结果；
- [ ] 当前内存和 CPU 基线；
- [ ] 当前毛玻璃截图；
- [ ] 当前通知截图；
- [ ] 当前贴边隐藏行为。

建议创建标签：

```bash
git tag electron-39-stable-baseline
```

---

## 15. 阶段 B：仅升级基础依赖

本阶段只修改：

- Electron；
- better-sqlite3；
- Koffi 版本固定；
- electron-builder 原生模块配置；
- macOS 通知失败处理。

禁止同时开发便利贴。

完成后验证：

```bash
npm run dev
npm run test
npm run test:db
npm run test:wallpaper-storage
npm run db:reset-business-schema
```

实际脚本名称以当前 `package.json` 为准。

---

## 16. 阶段 C：现有功能回归

### 16.1 主窗口

- [ ] 应用正常启动；
- [ ] 主窗口显示；
- [ ] 主窗口关闭后隐藏到托盘；
- [ ] 主窗口不会进入任务栏；
- [ ] 托盘重新打开主窗口；
- [ ] 最小化；
- [ ] 最大化；
- [ ] 自定义标题栏；
- [ ] 窗口位置保存；
- [ ] 窗口大小保存。

### 16.2 数据库

- [ ] 新数据库初始化；
- [ ] 旧数据库打开；
- [ ] 便签读取；
- [ ] 便签新增；
- [ ] 便签修改；
- [ ] 便签删除；
- [ ] 标签读取；
- [ ] 设置读取；
- [ ] WAL 模式；
- [ ] 数据库关闭；
- [ ] 数据库 Schema 重置测试；
- [ ] 打包后 `.node` 模块正常加载。

### 16.3 原生毛玻璃

- [ ] Windows 10 初始化；
- [ ] Windows 11 初始化；
- [ ] 模糊强度；
- [ ] 透明度；
- [ ] RGB 颜色；
- [ ] 窗口移动；
- [ ] 窗口缩放；
- [ ] 隐藏和恢复；
- [ ] 多次启用和禁用；
- [ ] 应用退出时资源释放。

### 16.4 贴边隐藏

- [ ] 左侧贴边；
- [ ] 右侧贴边；
- [ ] 失焦隐藏；
- [ ] 热区唤醒；
- [ ] 多显示器；
- [ ] DPI 缩放；
- [ ] Windows 任务栏位置变化；
- [ ] 主窗口关闭到托盘。

### 16.5 通知

- [ ] Windows 普通通知；
- [ ] Windows 富通知；
- [ ] 通知点击；
- [ ] 自定义协议；
- [ ] macOS 签名包通知；
- [ ] macOS 富通知动作按钮（稍后提醒 / 点击打开）；
- [ ] macOS 未签名包失败降级为应用内消息条；
- [ ] 主窗口隐藏时通知；
- [ ] 退出时取消调度器。

### 16.6 构建

- [ ] Windows x64 安装包；
- [ ] Windows portable；
- [ ] macOS x64；
- [ ] macOS arm64；
- [ ] macOS Universal，如需要；
- [ ] ASAR 内容；
- [ ] better-sqlite3 unpack；
- [ ] Koffi unpack；
- [ ] blur DLL 打包；
- [ ] 安装后首次启动；
- [ ] 升级安装；
- [ ] 卸载。

---

## 17. 升级完成标准

只有满足以下条件，才进入便利贴开发：

- 现有功能无阻断性回归；
- 数据库无损坏；
- Windows 原生毛玻璃正常；
- 贴边隐藏正常；
- 托盘生命周期正常；
- Windows 安装包正常；
- macOS 安装包正常；
- 主进程无新增持续错误日志；
- 打包后原生模块无 ABI 错误；
- Electron 43 分支可独立发布。

---

# 第四部分：桌面便利贴最终需求

## 18. 产品定位

用户可以从主页面的便签卡片中点击“贴到桌面”，基于当前便签正文创建一个独立桌面便利贴窗口。

核心定位：

> 一次性、临时、纯文字、即用即弃。

便利贴不是源便签的另一个编辑入口。

---

## 19. 生命周期

| 特性 | 最终规则 |
|---|---|
| 内容来源 | 创建时复制便签正文快照 |
| 与源便签关系 | 创建后彻底解耦 |
| 内容修改 | 只读，不允许修改 |
| 回写源便签 | 不回写 |
| 数据库存储 | 不存储 |
| 应用重启恢复 | 不恢复 |
| 单张关闭 | 销毁该便利贴，内容丢弃 |
| 主窗口关闭 | 主窗口隐藏，便利贴继续存在 |
| 托盘退出 | 关闭全部便利贴并退出应用 |
| 同源重复创建 | 允许 |
| 空白正文 | 禁止创建 |
| 内容类型 | 仅纯文字 |
| 图片与附件 | 不支持 |

主进程运行期间允许在内存中保留便利贴快照与运行状态，用于：

- 托盘菜单；
- 便利贴定位；
- 显示器迁移；
- Renderer 异常恢复；
- 资源释放。

“不持久化”指不写数据库和磁盘，而不是主进程完全不保存运行期状态。

---

## 20. 空白内容处理

创建前统一换行符：

```js
function normalizeStickyContent(value) {
  return String(value ?? '').replace(/\r\n?/g, '\n')
}
```

判断：

```js
const content = normalizeStickyContent(note.content)

if (!content.trim()) {
  showToast('便签内容为空，无法贴到桌面')
  return
}
```

只使用 `trim()` 判断是否为空，不使用裁剪后的结果作为正文，避免丢失用户原有缩进和换行。

建议设置单张便利贴正文安全上限：

```js
const MAX_STICKY_TEXT_LENGTH = 100_000
```

超过限制时拒绝创建并提示，不静默截断正文。

---

# 第五部分：便利贴窗口设计

## 21. 窗口形态

便利贴使用：

- 无边框；
- 不透明；
- 纯色背景；
- 直角；
- 自定义顶部导航栏；
- 不进入任务栏；
- 支持自由移动；
- 支持自由调整宽高；
- 支持置顶；
- 支持独立关闭。

推荐基础参数：

```js
{
  width: 280,
  height: 260,

  minWidth: 180,
  minHeight: 140,

  frame: false,
  transparent: false,
  roundedCorners: false,
  hasShadow: false,

  resizable: true,
  movable: true,
  minimizable: true,
  maximizable: false,
  fullscreenable: false,

  skipTaskbar: true,
  show: false
}
```

默认不使用：

- 透明窗口；
- 系统毛玻璃；
- CSS 毛玻璃；
- 圆角；
- 大面积阴影；
- 动态背景；
- 纹理图片。

为增加纸张边界感，可以使用：

```css
border: 1px solid rgba(0, 0, 0, 0.12);
```

不使用模糊阴影。

---

## 22. 自定义导航栏

推荐高度：

```css
--sticky-toolbar-height: 30px;
```

结构：

```html
<div class="sticky-window">
  <header class="sticky-toolbar">
    <div class="sticky-drag-area"></div>

    <div class="sticky-actions">
      <button data-action="font-down">A−</button>
      <button data-action="font-up">A+</button>
      <button data-action="color">颜色</button>
      <button data-action="pin">置顶</button>
      <button data-action="close">关闭</button>
    </div>
  </header>

  <main class="sticky-content"></main>
</div>
```

拖动区域：

```css
.sticky-toolbar,
.sticky-drag-area {
  app-region: drag;
}

.sticky-actions,
.sticky-actions button,
.sticky-actions input,
.sticky-content {
  app-region: no-drag;
}
```

正文区域必须允许：

- 选择；
- 复制；
- 滚动。

正文不可编辑。

---

## 23. 控件显示

导航栏本体始终存在，用于窗口拖动。

操作按钮默认弱化，鼠标悬停或键盘聚焦时显示：

```css
.sticky-actions {
  opacity: 0;
  pointer-events: none;
  transition: opacity 120ms ease;
}

.sticky-window:hover .sticky-actions,
.sticky-window:focus-within .sticky-actions {
  opacity: 1;
  pointer-events: auto;
}
```

禁止：

- 无限 CSS 动画；
- 呼吸动画；
- 持续闪烁；
- 持续 `requestAnimationFrame`；
- 定时器控制显隐。

---

## 24. 正文样式

```css
.sticky-content {
  overflow: auto;
  white-space: pre-wrap;
  overflow-wrap: anywhere;
  user-select: text;
  cursor: text;
}
```

滚动条：

- 默认弱化；
- 悬停时明显；
- 不使用第三方滚动库。

---

## 25. 字号

建议范围：

```text
最小：12px
默认：16px
最大：32px
步进：2px
```

字号属于单张便利贴运行状态：

- 不影响源便签；
- 不影响其他便利贴；
- 不写数据库；
- 应用退出后丢失。

---

## 26. 色板与颜色选择器

内置色板：

| 名称 | 色值 |
|---|---|
| 经典黄 | `#FFF2A8` |
| 杏橙 | `#FFD9A8` |
| 樱粉 | `#FFD4E1` |
| 天空蓝 | `#D4EAFF` |
| 薄荷绿 | `#DDF3D5` |
| 淡紫 | `#E8DEFF` |
| 米白纸 | `#F4F0E6` |
| 纯白 | `#FFFFFF` |

同时提供原生颜色选择器：

```html
<input type="color">
```

不引入第三方颜色选择器组件。

程序根据背景亮度自动选择黑色或白色文字：

```js
function getContrastTextColor(hexColor) {
  const hex = hexColor.replace('#', '')
  const r = Number.parseInt(hex.slice(0, 2), 16)
  const g = Number.parseInt(hex.slice(2, 4), 16)
  const b = Number.parseInt(hex.slice(4, 6), 16)

  const luminance =
    (0.2126 * r + 0.7152 * g + 0.0722 * b) / 255

  return luminance > 0.55 ? '#1F2328' : '#FFFFFF'
}
```

背景变化时同时更新：

- 页面根元素背景；
- 导航栏背景；
- 按钮对比度；
- 正文颜色；
- 原生窗口 `backgroundColor`。

---

# 第六部分：低占用技术架构

## 27. 最终技术路线

采用：

```text
Electron 43.2.0
BaseWindow
WebContentsView
webContents.clone()
原生 HTML/CSS/JavaScript
独立最小化 preload
```

Electron 43 的 `webContents.clone()` 会复制 WebPreferences，并使用相同 SiteInstance。克隆后的 WebContents 加载同源页面时，可以复用原 Renderer 进程；跨源导航时才会创建新的 Renderer。官方说明该方式可以在一定程度上降低内存占用并减少新 Renderer 初始化成本。

> 风险提示：“共享同一 Renderer”并非 100% 保证。Chromium 的进程模型受 process-per-site-instance 策略与进程数/内存上限影响：当达到进程数上限或内存压力较大时，Chromium 可能自动拆分或回收进程，导致同源便利贴出现多个 Renderer PID。验收时应将此视为预期行为，而非 bug；真实收益以§ 54 的方案 A/B 对比测试结果为准。

目标结构：

```text
Main Process
├─ Main BrowserWindow
├─ TrayManager
├─ StickyWindowManager
├─ StickyDisplayManager
├─ StickyPlacementManager
├─ StickyRegistry
│
├─ Sticky BaseWindow 1
│  └─ WebContentsView 1 ─┐
├─ Sticky BaseWindow 2   │
│  └─ WebContentsView 2 ─┼─ 共享 Sticky Renderer
├─ Sticky BaseWindow 3   │
│  └─ WebContentsView 3 ─┘
│
├─ SQLite
└─ NativeBlurManager
```

---

## 28. 为什么便利贴不使用 Vue

单张便利贴只需要：

- 显示文字；
- 改变字号；
- 改变颜色；
- 置顶；
- 关闭；
- 滚动。

不需要：

- Vue；
- Vue Router；
- Pinia；
- 组件库；
- Markdown；
- 富文本；
- 数据库；
- 业务 Store。

因此便利贴页面采用原生 HTML/CSS/JS。

推荐目录：

```text
src/
├─ main/
│  └─ sticky/
│     ├─ StickyWindowManager.js
│     ├─ StickyPlacementManager.js
│     ├─ StickyDisplayManager.js
│     ├─ StickyTrayMenu.js
│     └─ stickyConstants.js
│
├─ preload/
│  └─ sticky.js
│
└─ sticky/
   ├─ sticky.html
   ├─ sticky.css
   └─ sticky.js
```

---

## 29. WebContents 创建策略

第一张便利贴：

```js
const view = new WebContentsView({
  webPreferences: stickyWebPreferences
})
```

后续便利贴：

```js
const source = getLivingStickyWebContents()

const clonedContents = source
  ? source.clone()
  : null

const view = clonedContents
  ? new WebContentsView({ webContents: clonedContents })
  : new WebContentsView({
      webPreferences: stickyWebPreferences
    })
```

所有便利贴必须加载完全相同的页面地址。

开发环境：

```text
同一个 electron-vite 开发服务器地址
```

生产环境：

```text
同一个 sticky.html 文件地址
```

禁止便利贴导航到：

- 外部网站；
- 其他本地页面；
- 用户提供的 URL；
- `data:` 页面；
- 跨源页面。

必须监听：

```js
view.webContents.on('will-navigate', event => {
  event.preventDefault()
})

view.webContents.setWindowOpenHandler(() => ({
  action: 'deny'
}))
```

---

## 30. Renderer 共享验证

不能只假设 clone 生效，必须实际检查：

```js
const pid = view.webContents.getOSProcessId()
```

创建 10 张便利贴后记录：

```text
Sticky 1 Renderer PID
Sticky 2 Renderer PID
...
Sticky 10 Renderer PID
```

验收目标：

> 同源便利贴应尽量使用同一个 Renderer OS PID。

若出现多个 PID，检查：

- 页面 URL 是否完全一致；
- 是否发生跨源导航；
- WebPreferences 是否一致；
- 是否从同一个 SiteInstance 克隆；
- 是否有独立 partition；
- 是否有不同 session；
- 是否有开发服务器重定向。

---

## 31. BaseWindow 内容布局

`BaseWindow` 不会自动将 `WebContentsView` 铺满窗口。

必须同步布局：

```js
function layoutSticky(sticky) {
  const [width, height] = sticky.window.getContentSize()

  sticky.view.setBounds({
    x: 0,
    y: 0,
    width,
    height
  })
}

sticky.window.on('resize', () => {
  layoutSticky(sticky)
})
```

这里只执行简单的原生 bounds 更新，不进行：

- 数据库访问；
- 托盘菜单刷新；
- 业务 IPC；
- 防重叠计算；
- 位置重新排列。

---

## 32. WebContents 资源释放

Electron 官方明确说明：关闭 BaseWindow 时，WebContentsView 中的 WebContents 不会自动销毁，开发者必须显式关闭，否则会发生内存泄漏。

清理流程：

```js
sticky.window.on('closed', () => {
  stickyByWebContentsId.delete(sticky.view.webContents.id)
  stickyRegistry.delete(sticky.id)

  if (!sticky.view.webContents.isDestroyed()) {
    sticky.view.webContents.close()
  }

  sticky.window.removeAllListeners()
  rebuildTrayMenu()
})
```

清理完成后必须保证：

- Registry 中无记录；
- WebContents ID 映射被删除；
- WebContents 已 destroyed；
- Window 引用被释放；
- View 引用被释放；
- 托盘菜单不再显示该便利贴。

---

## 33. StickyRegistry

主进程内存注册表建议结构：

```js
{
  id,
  content,
  preview,

  window,
  view,
  webContentsId,

  bounds,
  displayId,

  fontSize,
  backgroundColor,
  textColor,
  pinned,

  createdAt,
  lastFocusedAt
}
```

不保存到数据库。

用途：

- 托盘列表；
- 聚焦单张便利贴；
- 显示全部；
- 显示器拔出迁移；
- Renderer 崩溃恢复；
- 运行期资源统计；
- 退出时统一释放。

---

## 34. IPC 设计

建议通道：

| 通道 | 方向 | 用途 |
|---|---|---|
| `sticky:create` | 主 Renderer → Main | 创建便利贴 |
| `sticky:init` | Main → Sticky | 初始化正文和样式 |
| `sticky:close` | Sticky → Main | 关闭自身 |
| `sticky:toggle-pin` | Sticky → Main | 切换置顶 |
| `sticky:update-appearance` | Sticky → Main | 用户完成字号或颜色修改后更新内存状态 |
| `sticky:get-state` | Sticky → Main | 必要时重新读取运行状态 |

不需要：

- 持续内容同步；
- 定时状态同步；
- 窗口位置轮询；
- 自定义缩放 IPC；
- 数据库 IPC；
- 源便签更新 IPC。

如果 Electron 43 的 scoped IPC 满足当前代码组织，可以优先使用：

```js
view.webContents.ipc.handle(...)
```

使 IPC 处理器只绑定当前便利贴 WebContents。

否则使用：

```js
const stickyByWebContentsId = new Map()
```

并根据：

```js
event.sender.id
```

定位对应便利贴。

禁止由 Renderer 传入可控制其他窗口的 sticky ID。

---

## 35. preload 最小接口

```js
contextBridge.exposeInMainWorld('stickyAPI', {
  close: () => ipcRenderer.invoke('sticky:close'),

  togglePin: () =>
    ipcRenderer.invoke('sticky:toggle-pin'),

  updateAppearance: state =>
    ipcRenderer.invoke('sticky:update-appearance', state),

  onInit: callback => {
    const handler = (_event, payload) => callback(payload)
    ipcRenderer.on('sticky:init', handler)

    return () => {
      ipcRenderer.removeListener('sticky:init', handler)
    }
  }
})
```

preload 中禁止暴露：

```js
send(channel, data)
invoke(channel, data)
on(channel, callback)
```

这类任意通道接口。

---

## 36. Sticky WebPreferences

```js
const stickyWebPreferences = {
  preload: stickyPreloadPath,

  nodeIntegration: false,
  contextIsolation: true,
  sandbox: true,

  devTools: false,
  spellcheck: false,
  plugins: false,
  webgl: false,

  backgroundThrottling: true
}
```

不要调用：

```js
app.disableHardwareAcceleration()
```

禁用 GPU 不必然降低占用，反而可能增加老旧 CPU 的绘制负担。是否禁用硬件加速必须通过单独性能测试决定。

---

# 第七部分：便利贴位置与多显示器

## 37. 新建显示器选择

新便利贴默认出现在鼠标所在显示器：

```js
const cursor = screen.getCursorScreenPoint()
const display = screen.getDisplayNearestPoint(cursor)
```

所有位置和尺寸使用 Electron DIP 坐标，不手动乘以 `scaleFactor`。

使用：

```js
display.workArea
```

而不是：

```js
display.bounds
```

避免覆盖任务栏、Dock 或系统保留区域。

---

## 38. 新建位置算法

推荐参数：

```js
const DEFAULT_WIDTH = 280
const DEFAULT_HEIGHT = 260
const PLACEMENT_GAP = 16
const SEARCH_STEP = 32
```

流程：

1. 取得鼠标所在显示器。
2. 以鼠标右下方为第一候选位置。
3. 若右下方越界，尝试左下、右上、左上。
4. 以 `SEARCH_STEP` 生成向外扩散的候选点。
5. 计算每个候选窗口与当前显示器已有便利贴的重叠面积。
6. 优先选择重叠面积为零的位置。
7. 如果全部存在重叠，选择总重叠面积最小的位置。
8. 重叠面积相同时，选择距离鼠标最近的位置。
9. 最终限制到 `workArea` 内。

候选评分可以使用：

```js
score =
  totalOverlapArea * 1_000_000 +
  distanceToCursorSquared
```

防重叠算法只在以下时机运行：

- 新建便利贴；
- 显示器被移除后的迁移。

用户主动拖动后允许便利贴互相重叠，不自动整理。

---

## 39. 显示器事件

监听：

```js
screen.on('display-added', ...)
screen.on('display-removed', ...)
screen.on('display-metrics-changed', ...)
```

`display-metrics-changed` 可以报告：

- bounds；
- workArea；
- scaleFactor；
- rotation。 


显示器事件应防抖：

```js
const DISPLAY_CHANGE_DEBOUNCE = 250
```

避免拔插显示器时系统连续触发多个拓扑变化事件。

---

## 40. 显示器移除处理

显示器被拔出时：

1. 等待显示器拓扑稳定。
2. 获取当前剩余显示器。
3. 找出位于已移除显示器的便利贴。
4. 根据原窗口中心点选择最近的剩余显示器。
5. 没有合适目标时使用主显示器。
6. 保留窗口在旧工作区中的相对位置。
7. 映射到新工作区。
8. 将窗口完整限制到新工作区内。
9. 多张便利贴发生完全重叠时执行轻微错位。

相对位置：

```js
const ratioX =
  (oldBounds.x - oldWorkArea.x) /
  Math.max(1, oldWorkArea.width - oldBounds.width)

const ratioY =
  (oldBounds.y - oldWorkArea.y) /
  Math.max(1, oldWorkArea.height - oldBounds.height)
```

映射：

```js
newX =
  newWorkArea.x +
  ratioX * (newWorkArea.width - width)

newY =
  newWorkArea.y +
  ratioY * (newWorkArea.height - height)
```

如果新工作区小于窗口：

- 优先缩小窗口；
- 不小于最小尺寸；
- 至少保证完整导航栏可见；
- 最终保证用户可以拖回和关闭窗口。

显示器重新连接后，不自动将便利贴搬回原显示器。

---

## 41. 工作区变化

以下情况也可能改变 `workArea`：

- Windows 任务栏移动；
- 任务栏自动隐藏设置变化；
- macOS Dock 位置变化；
- 分辨率变化；
- 缩放比例变化；
- 屏幕旋转。

发生变化后只处理完全不可见或标题栏不可访问的窗口，不强制重新排列所有便利贴。

---

# 第八部分：托盘总览

## 42. 基本原则

主程序和便利贴均不进入任务栏：

```js
skipTaskbar: true
```

唯一统一入口为系统托盘图标。

Electron 的 Tray 支持通过 `tray.setContextMenu()` 设置原生托盘菜单；菜单发生变化后可以重新构建并重新设置。

---

## 43. 托盘菜单结构

存在便利贴时：

```text
打开主窗口

显示全部便利贴（6）

便利贴总览（6） >
  完成 Electron 43 升级……
  明天上午十点开会……
  购买咖啡豆和牛奶……
  项目上线前检查……
  周末需要处理的事情……
  整理便利贴模块需求……

退出应用
```

不存在便利贴时：

```text
打开主窗口

显示全部便利贴（0）    [禁用]

便利贴总览（0） >
  暂无便利贴            [禁用]

退出应用
```

---

## 44. 便利贴菜单标题

使用正文中第一行非空内容：

```js
function createStickyMenuTitle(content, index) {
  const firstLine = String(content)
    .replace(/\r\n?/g, '\n')
    .split('\n')
    .map(line => line.trim())
    .find(Boolean)

  const title = firstLine || `便利贴 ${index + 1}`

  return title.length > 24
    ? `${title.slice(0, 24)}…`
    : title
}
```

菜单顺序建议固定为创建顺序。

不要根据最近聚焦时间动态调整菜单顺序，否则菜单项会不断跳动。

---

## 45. 点击单张便利贴

```js
function focusSticky(sticky) {
  const win = sticky.window

  if (win.isMinimized()) {
    win.restore()
  }

  win.show()
  win.moveTop()
  win.focus()

  sticky.lastFocusedAt = Date.now()
}
```

点击菜单项后：

- 恢复最小化；
- 显示隐藏窗口；
- 移到可见层；
- 获得焦点；
- 不移动位置；
- 不修改尺寸；
- 不修改颜色；
- 不修改置顶状态。

---

## 46. 显示全部便利贴

行为：

1. 恢复全部最小化便利贴。
2. 显示全部隐藏便利贴。
3. 对非目标便利贴使用 `showInactive()`。
4. 将便利贴带到可见层。
5. 最后只聚焦最近使用的一张。

示意：

```js
function showAllStickies() {
  const stickies = [...stickyRegistry.values()]

  if (stickies.length === 0) {
    return
  }

  for (const sticky of stickies) {
    const win = sticky.window

    if (win.isMinimized()) {
      win.restore()
    }

    win.showInactive()
    win.moveTop()
  }

  const target = [...stickies].sort(
    (a, b) => b.lastFocusedAt - a.lastFocusedAt
  )[0]

  target?.window.focus()
}
```

禁止对所有窗口连续调用 `focus()`，否则会出现窗口依次抢焦点和视觉闪烁。

---

## 47. 托盘菜单重建时机

只在以下事件发生时重建：

- 创建便利贴；
- 关闭便利贴；
- 应用语言切换；
- 必要的正文初始化完成；
- 托盘对象重新创建。

不在以下事件中重建：

- 窗口移动；
- 窗口缩放；
- 窗口聚焦；
- 改变字号；
- 改变颜色；
- 切换置顶；
- 正文滚动。

---

## 48. 大量便利贴菜单

原生托盘菜单不适合一次显示几十个同级菜单项。

超过 20 张时分组：

```text
便利贴总览（47） >
  第 1–20 张 >
  第 21–40 张 >
  第 41–47 张 >
```

每组保持固定创建顺序。

---

# 第九部分：应用退出规则

## 49. 最终生命周期

| 操作 | 主窗口 | 便利贴 | 应用进程 |
|---|---|---|---|
| 点击主窗口关闭 | 隐藏 | 保留 | 保留 |
| 关闭单张便利贴 | 不变 | 仅关闭该张 | 保留 |
| 托盘打开主窗口 | 显示并聚焦 | 不变 | 保留 |
| 托盘显示全部 | 不变 | 全部显示 | 保留 |
| 托盘退出 | 关闭 | 全部关闭 | 退出 |
| macOS 真正退出 | 关闭 | 全部关闭 | 退出 |
| 系统关机 | 关闭 | 全部关闭 | 退出 |

项目当前生命周期已经基本满足这一要求。

统一使用：

```js
let isQuitting = false

app.on('before-quit', () => {
  isQuitting = true
})
```

主窗口：

```js
mainWindow.on('close', event => {
  if (!isQuitting) {
    event.preventDefault()
    mainWindow.hide()
  }
})
```

托盘退出：

```js
function quitFromTray() {
  isQuitting = true
  app.quit()
}
```

应用真正退出时，统一调用：

```js
stickyWindowManager.destroyAll()
```

---

# 第十部分：便利贴性能要求

## 50. 页面禁止项

便利贴 Renderer 禁止使用：

- Vue；
- Pinia；
- Vue Router；
- Element Plus 等组件库；
- 主窗口完整 CSS；
- 网络字体；
- 网络图片；
- Markdown 解析器；
- 富文本解析器；
- Canvas；
- 视频；
- WebGL；
- 数据库；
- 持续轮询；
- `setInterval`；
- 持续 `requestAnimationFrame`；
- 无限 CSS 动画；
- 周期性 IPC；
- 高频日志；
- DOM 观察器轮询。

---

## 51. 允许的运行行为

只有用户操作时执行逻辑：

- 点击按钮；
- 修改字号；
- 修改颜色；
- 切换置顶；
- 滚动正文；
- 移动窗口；
- 缩放窗口；
- 聚焦窗口；
- 关闭窗口。

空闲时便利贴页面不应主动执行 JavaScript 任务。

---

## 52. 窗口数量限制

不限制同一源便签重复创建。

但不在产品层承诺无限数量。

建议：

```js
const MAX_STICKY_WINDOWS = 30
```

这是初始安全值，最终数值必须根据 Windows 10、Windows 11 和 macOS 性能测试调整。

达到上限时提示：

```text
当前桌面便利贴数量已达到上限，请关闭部分便利贴后重试。
```

不能继续创建导致旧电脑失去响应。

---

## 53. 性能测试规模

分别测试：

- 1 张；
- 10 张；
- 20 张；
- 30 张；
- 50 张压力测试。

记录：

- Electron 总进程数量；
- Renderer 进程数量；
- Sticky Renderer PID；
- 主进程内存；
- Sticky Renderer 内存；
- GPU 进程内存；
- 应用总内存；
- 空闲 CPU；
- 单张创建耗时；
- 连续创建耗时；
- 移动流畅度；
- 缩放流畅度；
- 托盘菜单打开速度；
- 显示全部响应速度；
- 显示器迁移速度；
- 关闭全部后的资源回收。

---

## 54. 性能验收条件

必须满足：

- [ ] 没有持续轮询；
- [ ] 没有周期性 IPC；
- [ ] 空闲时没有持续 JavaScript 活动；
- [ ] 10 张同源便利贴尽量共享一个 Renderer PID；
- [ ] 便利贴数量增加时，不按一张一个 Renderer 进程增长；
- [ ] 关闭便利贴后 WebContents 被销毁；
- [ ] Registry 不保留已关闭窗口；
- [ ] 所有便利贴关闭后 Sticky Renderer 可以退出；
- [ ] 托盘菜单不会随移动和聚焦频繁重建；
- [ ] 拔出副屏不会导致便利贴永久留在不可见区域；
- [ ] 旧电脑上达到安全数量前不出现明显输入卡顿；
- [ ] 达到安全上限后拒绝继续创建。

建议同时制作两份原型进行对比：

```text
方案 A：
普通 BrowserWindow，一张一个 Renderer

方案 B：
BaseWindow + WebContentsView + webContents.clone()
```

只有方案 B 确实降低总内存和进程数量后，才正式采用共享 Renderer 架构。

---

# 第十一部分：Renderer 异常处理

## 55. 共享 Renderer 风险

共享 Renderer 可以降低资源占用，但也意味着：

> 一个 Sticky Renderer 崩溃时，可能同时影响多张便利贴。

监听：

```js
view.webContents.on('render-process-gone', (_event, details) => {
  handleStickyRendererGone(details)
})
```

主进程内存注册表必须保留：

- 正文；
- 字号；
- 颜色；
- 置顶状态；
- bounds；
- displayId。

Renderer 崩溃后：

1. 标记受影响的便利贴。
2. 创建新的 Sticky Renderer。
3. 重新创建 WebContentsView。
4. 重新加载统一页面。
5. 根据内存状态恢复正文和样式。
6. 保留原窗口位置与置顶状态。
7. 不写入数据库。

Electron 43 可在 Renderer OOM 崩溃报告中提供 JavaScript 调用栈，有助于排查大量便利贴导致的内存问题。

---

# 第十二部分：开发步骤

## 56. 阶段 1：Electron 43 升级

- [ ] 创建升级分支；
- [ ] 固定 Electron 43.2.0；
- [ ] 固定 better-sqlite3 12.11.2；
- [ ] 固定 Koffi 2.16.2；
- [ ] 删除 `npmRebuild:false`；
- [ ] 增加原生模块 asarUnpack；
- [ ] 更新 package-lock；
- [ ] 重新安装依赖；
- [ ] 验证 Electron 下载镜像；
- [ ] 增加通知 failed 监听；
- [ ] 完成现有功能回归；
- [ ] 完成 Windows/macOS 打包；
- [ ] 提交独立升级 commit。

建议提交：

```text
chore: upgrade Electron from 39 to 43
```

---

## 57. 阶段 2：便利贴共享 Renderer 原型

只实现：

- 创建窗口；
- 显示静态文字；
- clone WebContents；
- 关闭；
- 资源销毁；
- 记录 Renderer PID。

暂时不实现：

- 色板；
- 托盘列表；
- 多显示器；
- 置顶；
- 字号；
- 完整 UI。

验收：

- 10 张便利贴共享 Renderer；
- 关闭后无 WebContents 泄漏；
- 内存明显优于普通 BrowserWindow 方案。

---

## 58. 阶段 3：StickyWindowManager

实现：

- Registry；
- 创建；
- 关闭；
- 关闭全部；
- WebContents ID 映射；
- scoped IPC；
- Renderer 崩溃恢复；
- 运行指标；
- 最大数量限制。

---

## 59. 阶段 4：便利贴 UI

实现：

- 自定义导航栏；
- 拖动区；
- 正文滚动；
- 字号增减；
- 色板；
- 原生颜色选择器；
- 自动文字对比色；
- 置顶；
- 关闭；
- hover 控件；
- 直角纯色外观。

---

## 60. 阶段 5：主窗口入口

在 `NoteCard.vue` 中增加：

```text
贴到桌面
```

创建前：

- 读取当前正文；
- 规范化换行；
- 判断空内容；
- 检查内容长度；
- 检查便利贴数量上限；
- 调用 `sticky:create`。

---

## 61. 阶段 6：托盘总览

实现：

- 显示全部便利贴；
- 便利贴总览；
- 单张聚焦；
- 数量显示；
- 超过 20 张分组；
- 创建和关闭时重建菜单；
- 无便利贴时禁用菜单项。

---

## 62. 阶段 7：多显示器

实现：

- 鼠标所在屏幕创建；
- 候选位置评分；
- 防重叠；
- workArea 限制；
- display-added；
- display-removed；
- display-metrics-changed；
- 显示器拔出迁移；
- DPI 与工作区变化。

---

## 63. 阶段 8：完整性能测试

测试：

- Windows 10；
- Windows 11；
- macOS Intel；
- macOS Apple Silicon；
- 单屏；
- 双屏；
- 三屏；
- 不同 DPI；
- 断开副屏；
- 30 张正常使用；
- 50 张压力场景；
- Renderer 强制崩溃；
- 托盘退出；
- 系统关机。

---

# 第十三部分：发布前最终检查表

## Electron 43

- [ ] Electron 固定为 43.2.0；
- [ ] better-sqlite3 固定为 12.11.2；
- [ ] Koffi 固定为 2.16.2；
- [ ] package-lock 已提交；
- [ ] 原生模块成功重建；
- [ ] ASAR unpack 正确；
- [ ] Windows DLL 正确打包；
- [ ] Electron 下载镜像正常；
- [ ] 未使用 `ELECTRON_SKIP_BINARY_DOWNLOAD`；
- [ ] macOS 通知失败可降级；
- [ ] 所有旧功能完成回归。

## 便利贴

- [ ] 禁止空白便利贴；
- [ ] 只展示纯文字；
- [ ] 不编辑、不回写；
- [ ] 不落库、不恢复；
- [ ] 主窗口关闭时便利贴保留；
- [ ] 托盘退出时全部关闭；
- [ ] 不进入任务栏；
- [ ] 直角、不透明、无玻璃；
- [ ] 自定义导航栏；
- [ ] 字号可调；
- [ ] 内置色板；
- [ ] 原生颜色选择器；
- [ ] 自动文字对比色；
- [ ] 支持置顶；
- [ ] 支持滚动与复制；
- [ ] 新建位置避免重叠；
- [ ] 拔出显示器后自动迁移；
- [ ] 托盘显示便利贴数量；
- [ ] 托盘可以显示全部；
- [ ] 托盘可以聚焦单张；
- [ ] 关闭后无 WebContents 泄漏；
- [ ] 多张尽量共享 Renderer；
- [ ] 达到安全数量后禁止继续创建。

---

# 第十四部分：最终决策

本项目最终采用以下技术路线：

```text
Electron 43.2.0

现有主窗口：
BrowserWindow + Vue 3

桌面便利贴：
BaseWindow
+ WebContentsView
+ webContents.clone()
+ 原生 HTML/CSS/JavaScript
+ 独立最小 preload

窗口外观：
不透明
纯色
直角
无系统毛玻璃
无任务栏入口
自定义导航栏

应用入口：
系统托盘
├─ 打开主窗口
├─ 显示全部便利贴（N）
├─ 便利贴总览（N）
│  └─ 点击单张聚焦
└─ 退出应用

生命周期：
关闭主窗口 → 仅隐藏
关闭单张便利贴 → 仅销毁该张
托盘退出 → 关闭全部并退出
应用重启 → 不恢复便利贴
```

实施原则：

> 先完成 Electron 43 升级和现有功能回归，再开发便利贴；先验证共享 Renderer 的真实资源收益，再实现完整 UI 和多屏幕逻辑。
